/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://material-ui.com','https://images.unsplash.com'],
  },
}

module.exports = nextConfig
